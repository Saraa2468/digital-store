apiVersion: v1
kind: Service
metadata:
  name: digital-store-service
spec:
  selector:
    app: digital-store
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 80   
  type: LoadBalancer